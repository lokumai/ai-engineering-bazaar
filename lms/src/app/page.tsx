/**
 * Placeholder index. The index sheet — the 56px title, the four-line
 * statement and the 32-row manifest — is spec §4.8 and lands with the home
 * page task; the shell renders the <main> landmark around whatever is here.
 */
export default function HomePage() {
  return (
    <h1 className="font-display text-h1 font-bold tracking-[-0.020em]">
      AI Engineering Bazaar
    </h1>
  )
}
